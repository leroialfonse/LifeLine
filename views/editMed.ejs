<%- include('partials/header') -%>

<div class="doc">
  <div class="col-6 justify-content-center">

    <label for="ul"><h2>Edit Medication Information</h2></label>
      <ul class="row list-unstyled justify-content-between col-6 mt-5 docEdit">
        <% if (meds.length === 0) { %>
          <p> No medications in your cabinet. Please add one to save it.</p>
          <%} else { %>
           <% } %> 
          
        <% meds.forEach( el => { %>  
          
          <li data-id='<%el._id%>'>
            <% if (el._id == idMed ) { %>
              <br>
            <form action="/editMed/update/<%= el._id%>" method="POST">
              <label for="title">Change Name:</label>
              <input type="text" name="title" value="<%= el.title %>">
              <label for="phone">Change Number:</label>
              <input type="text" name = "phone" value = "<%= el.phone %>">
              <label for="address">Change Address:</label>
              <input type="text" name="address" value="<%= el.address %>">
              <button btn-primary class="btn-primary fa fa-check" type="submit"></button>
              <a href="back">Cancel</a>
            </form>
            <br>
          </li>
          
        <% } else { %>
                            <li>

                                <div><%= el.title %></div>
                                <div><%= el.address %></div>
                                <div><%= el.phone %></div>

                                <a href="/editMed/<%= el._id %>">Edit</a>
                                <a href="/editMed/remove/<%= el._id %>">Remove</a>
                            </li>
                            <br>
                            <% } %>
                    <% }) %>
                </ul>
        
              </div>

              </div>